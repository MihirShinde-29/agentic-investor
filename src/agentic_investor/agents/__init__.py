"""Analysis agents as LangGraph nodes.

Each agent returns a structured Pydantic signal instead of free text, so the
orchestrator can combine them programmatically. Technical (M1), news (M2);
fundamentals and catalyst are on the roadmap.
"""
