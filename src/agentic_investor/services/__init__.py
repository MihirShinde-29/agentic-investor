"""Subprocess services spawned by the paper-experiment supervisor.

Sibling packages to the arm processes: shared, single-purpose, consumed
over localhost HTTP or a shared sqlite bus. Existing peers under this
same shape: paper-news-bus, paper-price-bus.
"""
